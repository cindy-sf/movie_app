{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
      "$src/*": ["src/*"],
      "$styles": ["src/styles/theme"],
      "$assets/*": ["src/assets/*"]
    }
  }
}
