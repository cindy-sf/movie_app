{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "src",
    "paths": {
      "$src": ["./src/*"],
      "$assets": ["./src/assets/*"],
      "$styles": ["styles/theme"]
    }
  }
}
